---
phase: 00-spine-interface-freeze
plan: 04
type: execute
wave: 2
depends_on: ["00-02"]
files_modified:
  - vitest.config.ts
  - playwright.config.ts
  - lighthouserc.json
  - scripts/i18n-check.ts
  - scripts/i18n-unused.ts
  - tests/i18n/routing.test.ts
  - tests/_setup/supabase-ephemeral.ts
  - e2e/pairing.spec.ts
  - e2e/fixtures/stub-bot.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [REQ-functional, REQ-user-journeys, REQ-mvp-scope]

must_haves:
  truths:
    - "Vitest runs and a sample i18n routing test passes"
    - "The i18n key-parity check fails CI when a DE/EN key is missing or extra"
    - "Playwright config + a UJ-002 pairing spec scaffold exist with a stub Telegram bot fixture"
    - "Lighthouse CI enforces the ADR-0020 budgets"
    - "CI runs lint → typecheck → i18n-parity → vitest → playwright → lhci on every PR"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest + coverage-v8 config"
      contains: "defineConfig"
    - path: "scripts/i18n-check.ts"
      provides: "DE/EN key-parity gate that exits non-zero on drift"
      contains: "process.exit"
    - path: "playwright.config.ts"
      provides: "Playwright config targeting the dev/preview server"
      contains: "defineConfig"
    - path: "lighthouserc.json"
      provides: "LCP/INP/CLS/first-load-JS budgets per ADR-0020"
      contains: "categories"
    - path: "e2e/pairing.spec.ts"
      provides: "UJ-002 pairing journey scaffold (stub bot), the merge-gate test"
      contains: "test("
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline: lint, typecheck, i18n-parity, vitest, playwright, lhci"
      contains: "i18n"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/i18n-check.ts"
      via: "CI step runs the key-parity gate"
      pattern: "i18n-check"
    - from: "e2e/pairing.spec.ts"
      to: "e2e/fixtures/stub-bot.ts"
      via: "import stub bot to simulate /start CODE"
      pattern: "stub-bot"
---

<objective>
Wire all four CI gates (Vitest, Playwright, Lighthouse CI, i18n key-parity) and lay the Wave-0 test scaffolds that every later Phase-0 plan's `<automated>` verifications depend on. Per the Validation Architecture, ALL test infrastructure is greenfield — this plan creates it so the pairing E2E (the exit gate), the edge-fn contract tests, and the schema-RLS tests have a place to run.

The Playwright UJ-002 pairing spec is scaffolded here (with a stub Telegram bot fixture per DEC-0020) and fully wired in 00-06 once the edge fns + Hermes exist. The i18n key-parity gate is fully functional now (it gates DE↔EN catalogs from 00-02).

Purpose: Establish the green-CI definition-of-done (DEC-0020) before features land, and avoid the Pitfall 7 trap of setting perf budgets too late.
Output: 4 gate configs + i18n scripts + test scaffolds + the CI workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-spine-interface-freeze/00-RESEARCH.md
@.planning/phases/00-spine-interface-freeze/00-VALIDATION.md
@docs/adr/0020-testing-strategy-and-performance-budgets.md
@docs/adr/0012-i18n-next-intl-de-authoritative.md
@.planning/phases/00-spine-interface-freeze/00-02-SUMMARY.md

<notes>
- Pinned (VERIFIED 2026-05-30): vitest 4.1.7 + @vitest/coverage-v8 · @playwright/test 1.60.0 · @lhci/cli 0.15.1. Install as devDeps if 00-02 didn't.
- ADR-0020 budgets: LCP <2.0s (Slow-4G), INP <200ms p75, CLS <0.1, first-load JS <90KB landing / <150KB account, Lighthouse ≥90 mobile / ≥95 desktop.
- DEC-0020: Playwright simulates Telegram pairing via a STUB BOT (CI path); the real bot proves the slice once (manual, in 00-06). 8-10 E2E journeys are the merge gate to main — Phase 0 only needs the pairing journey.
- i18n: `scripts/i18n-check.ts` is a CUSTOM but specified key-parity gate that FAILS CI on missing/extra keys (DEC-0012). `scripts/i18n-unused.ts` is warn-only (PR comment, non-blocking). Use a clean grep gate (filter comments) — never a self-invalidating count.
- A9: CI boots an EPHEMERAL local Supabase (Supabase CLI) for edge-fn/db integration tests — distinct from the self-hosted prod VPS. Create `tests/_setup/supabase-ephemeral.ts` as the harness hook (the actual schema-RLS + edge-fn tests are authored in 00-03/00-05 against it).
- A10: Lighthouse CI runs against the Vercel preview deployment per PR (LHCI + Vercel integration).
- Do NOT use watch-mode flags in CI (`vitest run`, not `vitest`).
</notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vitest config + i18n key-parity gate + routing test</name>
  <files>vitest.config.ts, scripts/i18n-check.ts, scripts/i18n-unused.ts, tests/i18n/routing.test.ts</files>
  <read_first>
    @docs/adr/0012-i18n-next-intl-de-authoritative.md
    @.planning/phases/00-spine-interface-freeze/00-02-SUMMARY.md (catalog locations, routing config)
  </read_first>
  <behavior>
    - i18n-check.ts: flattens every messages/de/*.json and messages/en/*.json namespace; exits 1 if any key exists in one locale but not the other (missing OR extra); exits 0 on perfect parity. Prints the offending keys.
    - i18n-unused.ts: warns (exit 0) listing keys present in catalogs but not referenced in src/ — non-blocking.
    - routing.test.ts: asserts routing.defaultLocale === 'de', routing.locales === ['de','en'], localePrefix 'always'; asserts the wordmark literal 'anchor' does NOT appear in any de/en catalog value.
  </behavior>
  <action>
    Create `vitest.config.ts` (`defineConfig` from `vitest/config`) with the v8 coverage provider, `globals:true`, a `tests/**` include, and a coverage threshold scaffold (lines ≥70 on critical paths — start permissive, tighten as tests land). Add `test` (`vitest`), `test:run` (`vitest run --coverage`) scripts to package.json.

    Create `scripts/i18n-check.ts`: read all `messages/de/*.json` + `messages/en/*.json`, flatten to dot-keys per namespace, diff the two locales, print missing/extra keys, `process.exit(1)` on any drift else `process.exit(0)`. Create `scripts/i18n-unused.ts` as the warn-only counterpart. Add `i18n:check` (`tsx scripts/i18n-check.ts`) + `i18n:unused` scripts.

    Create `tests/i18n/routing.test.ts` per `<behavior>` (imports `routing` from `src/i18n/routing.ts`; reads the catalogs for the wordmark assertion).
  </action>
  <verify>
    <automated>grep -q 'defineConfig' vitest.config.ts && grep -q 'process.exit(1)' scripts/i18n-check.ts && grep -q "defaultLocale" tests/i18n/routing.test.ts && npx vitest run tests/i18n/routing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run tests/i18n/routing.test.ts` passes (routing config + no-wordmark-in-catalog assertions).
    - `scripts/i18n-check.ts` exits 1 on a deliberately introduced key mismatch and 0 on parity (provable by temporarily editing a catalog — revert after).
    - `scripts/i18n-unused.ts` is warn-only (always exit 0).
    - Grep gates filter comments (no self-invalidating count).
  </acceptance_criteria>
  <done>Vitest runs, the i18n key-parity gate is a real CI failure on drift, and the routing test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright config + UJ-002 pairing scaffold + stub bot + ephemeral Supabase harness</name>
  <files>playwright.config.ts, e2e/pairing.spec.ts, e2e/fixtures/stub-bot.ts, tests/_setup/supabase-ephemeral.ts</files>
  <read_first>
    @docs/adr/0020-testing-strategy-and-performance-budgets.md
    @docs/requirements/user-journeys.md (UJ-002 acceptance criteria)
    @docs/adr/0007-single-shared-telegram-bot-with-pairing-code.md
  </read_first>
  <action>
    Create `playwright.config.ts` (`defineConfig` from `@playwright/test`) with a `webServer` launching the Next dev/preview server, a single project (chromium), and `e2e/` as testDir.

    Create `e2e/fixtures/stub-bot.ts`: a stub Telegram bot that, given a pairing code, simulates `/start <code>` by calling the `pairing-redeem` edge fn the same way real Hermes will (service-role + x-hermes-secret) — for now export the function signature + a fetch shape; it is fully wired in 00-06 against the deployed stubs.

    Create `e2e/pairing.spec.ts`: the UJ-002 journey skeleton — `test('UJ-002: login → pairing code shown → stub /start redeem → connected via realtime', ...)` with the steps as comments/`test.fixme` placeholders mapping to: (1) magic-link login, (2) pairing code rendered, (3) stub bot redeems, (4) UI flips to "connected" via Realtime. It must reference `stub-bot`. Mark the assertions `test.fixme` until 00-06 implements them so CI stays green now but the spec file is the merge-gate target.

    Create `tests/_setup/supabase-ephemeral.ts`: the harness hook that boots an ephemeral local Supabase (Supabase CLI) and exposes its connection string for integration tests (used by 00-03 schema-RLS tests + 00-05 edge-fn tests). Document the A9 caveat (CLI local stack ≈ self-host) in a header comment.
  </action>
  <verify>
    <automated>grep -q 'defineConfig' playwright.config.ts && grep -q "test(" e2e/pairing.spec.ts && grep -q 'stub-bot' e2e/pairing.spec.ts && test -f tests/_setup/supabase-ephemeral.ts && npx playwright test --list e2e/pairing.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `playwright test --list` discovers `e2e/pairing.spec.ts`.
    - `e2e/pairing.spec.ts` is the UJ-002 journey scaffold referencing `e2e/fixtures/stub-bot.ts`, with assertions `test.fixme` pending 00-06.
    - `e2e/fixtures/stub-bot.ts` exports the stub-redeem signature mirroring Hermes' service-role + x-hermes-secret call.
    - `tests/_setup/supabase-ephemeral.ts` boots/exposes an ephemeral local Supabase for integration tests.
  </acceptance_criteria>
  <done>Playwright is configured, the UJ-002 pairing spec is the (pending) merge-gate target, the stub bot + ephemeral-Supabase harness exist.</done>
</task>

<task type="auto">
  <name>Task 3: Lighthouse budgets + CI workflow wiring all gates</name>
  <files>lighthouserc.json, .github/workflows/ci.yml</files>
  <read_first>
    @docs/adr/0020-testing-strategy-and-performance-budgets.md (budgets)
    @.planning/phases/00-spine-interface-freeze/00-VALIDATION.md (full suite command)
  </read_first>
  <action>
    Create `lighthouserc.json`: assert categories (performance ≥0.90 mobile), resource-summary budgets for first-load JS (<90KB landing route, <150KB account route — encode as `resourceSizes` script budgets), and Core Web Vitals assertions (LCP <2000ms, CLS <0.1, INP <200ms via `total-blocking-time`/`interaction-to-next-paint` where supported). Point `collect.url` at the Vercel preview (configurable via env) and the `/de` shell route.

    Create `.github/workflows/ci.yml`: on pull_request + push to main, a job that runs (in order): `pnpm install`, `lint`, `typecheck` (`tsc --noEmit`), `pnpm i18n:check` (key-parity — FAILS the build on drift), `pnpm test:run` (vitest + coverage), `pnpm exec playwright test` (with the ephemeral-Supabase boot step from `tests/_setup`), and `pnpm exec lhci autorun`. Each gate is a required step; the build is red if any fails. Cache pnpm + Playwright browsers.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('lighthouserc.json','utf8'))" && grep -q 'i18n' .github/workflows/ci.yml && grep -q 'playwright' .github/workflows/ci.yml && grep -q 'lhci' .github/workflows/ci.yml && grep -q 'vitest\|test:run' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `lighthouserc.json` is valid JSON encoding the four ADR-0020 budgets (LCP/INP/CLS + first-load JS) and a ≥90 mobile category assertion.
    - `.github/workflows/ci.yml` runs lint → typecheck → i18n:check → vitest → playwright → lhci, each as a required step, on PR + push to main.
    - The i18n:check step fails the build on DE/EN key drift.
  </acceptance_criteria>
  <done>All four CI gates are wired into a single workflow that defines green-CI for every PR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author → CI pipeline | Untrusted PR code runs in CI; secrets must not leak to forks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-11 | Information Disclosure | Real secrets (service-role, x-hermes-secret) in CI | mitigate | CI uses the ephemeral local Supabase + stub bot, NOT prod secrets; LHCI runs against the Vercel preview. No prod service-role key in the workflow. |
| T-00-12 | Tampering | i18n drift / missing translations shipping silently | mitigate | `i18n:check` is a required, build-failing CI step; key drift cannot merge. |
| T-00-13 | Denial of Service | Watch-mode flag hanging CI | mitigate | CI uses `vitest run` / `playwright test` (non-watch); validated by the workflow steps. |
</threat_model>

<verification>
- `vitest run tests/i18n/routing.test.ts` passes; `i18n-check.ts` fails on drift, passes on parity.
- `playwright test --list` discovers the pairing spec; the stub bot + ephemeral-Supabase harness exist.
- `lighthouserc.json` is valid JSON with the four budgets; `ci.yml` wires all gates as required steps.
</verification>

<success_criteria>
CI is wired and green: Vitest, Playwright (the pending UJ-002 pairing journey, ready for 00-06), Lighthouse budgets, and i18n key-parity all run on every PR; the i18n gate fails the build on DE/EN drift; the perf budgets are enforced from the empty shell so features can't silently blow them.
</success_criteria>

<output>
After completion, create `.planning/phases/00-spine-interface-freeze/00-04-SUMMARY.md` recording the gate configs, the CI step order, the ephemeral-Supabase harness, and the pending UJ-002 spec location.
</output>
