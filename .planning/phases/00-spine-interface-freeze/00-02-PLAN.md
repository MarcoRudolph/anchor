---
phase: 00-spine-interface-freeze
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - eslint.config.mjs
  - src/proxy.ts
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/app/globals.css
  - src/components/brand/FlowerIcon.tsx
  - src/components/brand/Wordmark.tsx
  - messages/de/common.json
  - messages/en/common.json
  - components.json
autonomous: true
requirements: [REQ-functional, REQ-mvp-scope, REQ-user-journeys]

must_haves:
  truths:
    - "A visitor hitting / is redirected to /de and sees the Anchor app shell with Rudolpho-AI branding"
    - "Switching to /en renders the English app shell from the en message catalog"
    - "The locale middleware is active (proxy.ts on Next 16, not middleware.ts)"
    - "The brand wordmark 'anchor' is rendered from a component, never from a locale file"
  artifacts:
    - path: "src/proxy.ts"
      provides: "next-intl v4 locale middleware (Next 16 filename)"
      contains: "createMiddleware"
    - path: "src/i18n/routing.ts"
      provides: "defineRouting with locales de/en, defaultLocale de, localePrefix always"
      contains: "defineRouting"
    - path: "src/app/[locale]/layout.tsx"
      provides: "App shell: Unbounded font, NextIntlClientProvider, branding"
      contains: "NextIntlClientProvider"
    - path: "src/components/brand/FlowerIcon.tsx"
      provides: "7-circle Flower-of-Life SVG using currentColor"
      contains: "circle"
    - path: "messages/de/common.json"
      provides: "Authoritative German message catalog"
      min_lines: 3
  key_links:
    - from: "src/proxy.ts"
      to: "src/i18n/routing.ts"
      via: "createMiddleware(routing)"
      pattern: "createMiddleware\\(routing\\)"
    - from: "src/i18n/request.ts"
      to: "messages/de/common.json"
      via: "dynamic import of locale messages"
      pattern: "messages/\\$\\{locale\\}"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/brand/Wordmark.tsx"
      via: "import + render in shell header"
      pattern: "Wordmark"
---

<objective>
Scaffold the Anchor webapp on Next.js 16 with Tailwind v4 (CSS-first), shadcn/ui, magic-ui readiness, next-intl v4 subpath routing (DE authoritative, EN secondary), and the Rudolpho-AI brand shell (Flower-of-Life icon + Unbounded wordmark per DESIGN.md). This is the empty-but-correct foundation every later Anchor plan builds on.

LOCKED DECISION: Next.js 16.2.6 (NOT 15). The locale middleware lives in `src/proxy.ts` (NOT `middleware.ts` — a `middleware.ts` is silently ignored on Next 16). next-intl is v4 (`defineRouting` + `createMiddleware(routing)` + `getRequestConfig({requestLocale})`). Tailwind is v4 (CSS-first `@theme` in globals.css, no `tailwind.config.js`).

Purpose: Freeze the scaffold shape (per DEC-0011/0012) so feature waves never re-negotiate the app skeleton, routing, i18n wiring, or branding.
Output: A running Next 16 app shell at /de and /en with branding, no feature logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-spine-interface-freeze/00-RESEARCH.md
@docs/adr/0011-nextjs-on-vercel-with-tailwind-and-shadcn.md
@docs/adr/0012-i18n-next-intl-de-authoritative.md

<notes>
- The repo-root `tsconfig.json` and `eslint.config.mjs` are STRAY `@ljharb` library configs (not Next.js). DELETE/REPLACE them — `create-next-app` produces correct ones. Do not extend the existing ones.
- Brand system (CLAUDE.md / DESIGN.md): FlowerIcon = 7-circle sacred geometry, stroke-only, `currentColor`, `viewBox="3 2 34 36"`. `.spin-slow` keyframe (10s linear infinite). Wordmark = Unbounded weight 900, `text-xl`, `tracking-tight`, lowercase, never in a locale file. Icon = `text-primary-500` (#FF6B00), `size-7`, `gap-2.5`.
- Pinned versions (VERIFIED 2026-05-30): next 16.2.6 · next-intl 4.13.0 · tailwindcss 4.3.0 · shadcn CLI 4.8.3 · @supabase/supabase-js 2.106.2 · @supabase/ssr 0.10.3 · drizzle-orm 0.45.2 · drizzle-kit 0.31.10 · stripe 22.2.0 · resend 6.12.4 · @react-email/components (latest). Install these (or matching caret) — re-run `npm view` only if planning slipped >7 days.
- Tailwind 4 is CSS-first: theme tokens via `@theme` in `src/app/globals.css`, NO `tailwind.config.js`.
- This plan owns the single `pnpm add` of the runtime deps the whole phase needs (per RESEARCH.md install block). `stripe`/`resend`/`@react-email/components` are installed HERE so 00-05 (which imports `stripe` + Resend) needs no package.json mutation.
</notes>

<interfaces>
<!-- next-intl v4 CITED pattern (00-RESEARCH.md). Use these directly. -->

src/i18n/routing.ts:
```ts
import { defineRouting } from 'next-intl/routing';
export const routing = defineRouting({
  locales: ['de', 'en'],
  defaultLocale: 'de',
  localePrefix: 'always',     // /de /en; bare / -> /de
});
```

src/proxy.ts (Next 16 — was middleware.ts pre-16; a middleware.ts is IGNORED on Next 16):
```ts
import createMiddleware from 'next-intl/middleware';
import { routing } from './i18n/routing';
export default createMiddleware(routing);
export const config = { matcher: '/((?!api|trpc|_next|_vercel|.*\\..*).*)' };
```

src/i18n/request.ts:
```ts
import { getRequestConfig } from 'next-intl/server';
import { hasLocale } from 'next-intl';
import { routing } from './routing';
export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale;
  const locale = hasLocale(routing.locales, requested) ? requested : routing.defaultLocale;
  return { locale, messages: (await import(`../../messages/${locale}/common.json`)).default };
});
```
Wire the plugin in next.config: `createNextIntlPlugin('./src/i18n/request.ts')`.

DESIGN.md FlowerIcon (copy verbatim, stroke-only, currentColor):
```tsx
function FlowerIcon({ className }: { className?: string }) {
  return (
    <svg viewBox="3 2 34 36" fill="none" stroke="currentColor" strokeWidth="1.4" className={className}>
      <circle cx="20" cy="20" r="9" strokeOpacity="0.9" />
      <circle cx="20" cy="11" r="9" strokeOpacity="0.65" />
      <circle cx="27.8" cy="15.5" r="9" strokeOpacity="0.65" />
      <circle cx="27.8" cy="24.5" r="9" strokeOpacity="0.65" />
      <circle cx="20" cy="29" r="9" strokeOpacity="0.65" />
      <circle cx="12.2" cy="24.5" r="9" strokeOpacity="0.65" />
      <circle cx="12.2" cy="15.5" r="9" strokeOpacity="0.65" />
    </svg>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next 16 + Tailwind v4 + shadcn, replace stray configs</name>
  <files>package.json, next.config.ts, tsconfig.json, eslint.config.mjs, components.json, src/app/globals.css</files>
  <read_first>
    @.planning/phases/00-spine-interface-freeze/00-RESEARCH.md (Standard Stack install block; Tailwind 4 CSS-first; Pitfall 6 proxy.ts)
    @docs/adr/0011-nextjs-on-vercel-with-tailwind-and-shadcn.md
  </read_first>
  <action>
    Delete the stray repo-root `tsconfig.json` and `eslint.config.mjs` (they are unrelated `@ljharb` library configs). Scaffold the Next 16 app IN PLACE at repo root using `pnpm create next-app@latest . --typescript --app --tailwind --eslint --src-dir --import-alias "@/*"` (Next 16.2.6, Tailwind 4). Then `pnpm add next-intl@4.13.0 @supabase/supabase-js@2.106.2 @supabase/ssr@0.10.3 drizzle-orm@0.45.2 zod stripe@22.2.0 resend@6.12.4 @react-email/components` and `pnpm add -D drizzle-kit@0.31.10`. (`stripe` is the customer-at-signup hook consumed by 00-05 Task 3 per DEC-0016; `resend` + `@react-email/components` are the magic-link/pairing email path per DEC-0017 — install them here so the email/Stripe path in 00-05 has no package.json mutation.) Run `npx shadcn@latest init` (4.8.3, Tailwind-4 aware) producing `components.json`. Add the `db:migrate` script (`bash scripts/db-migrate.sh`) to package.json scripts (the runner is created in 00-01).

    Wire next-intl into `next.config.ts` with `createNextIntlPlugin('./src/i18n/request.ts')`. In `src/app/globals.css` add the Tailwind v4 `@theme` block with the brand primary token `--color-primary-500: #FF6B00;` and the `.spin-slow` keyframe (10s linear infinite per DESIGN.md). Do NOT create a `tailwind.config.js`.

    Confirm the scaffolded `tsconfig.json` is a Next.js config (has `"jsx": "preserve"`, `"plugins": [{ "name": "next" }]`) — not the deleted @ljharb one.
  </action>
  <verify>
    <automated>test -f package.json && grep -q '"next":' package.json && grep -q '"stripe":' package.json && grep -q '"resend":' package.json && grep -q 'createNextIntlPlugin' next.config.ts && grep -q 'spin-slow' src/app/globals.css && ! test -f tailwind.config.js</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists with `next` 16.x, `next-intl` 4.x, `@supabase/ssr`, `drizzle-orm`, `drizzle-kit`, `zod` at the pinned versions, and a `db:migrate` script.
    - `package.json` also lists `stripe` (22.x, DEC-0016 customer-at-signup, consumed by 00-05) and `resend` (6.x, DEC-0017 email path) as runtime dependencies (assert via `grep -q '"stripe":' package.json` and `grep -q '"resend":' package.json`); `@react-email/components` is present for the React Email path.
    - `next.config.ts` wires `createNextIntlPlugin('./src/i18n/request.ts')`.
    - `src/app/globals.css` has a Tailwind v4 `@theme` block with `--color-primary-500: #FF6B00` and a `.spin-slow` keyframe; NO `tailwind.config.js` exists.
    - The stray `@ljharb` `tsconfig.json`/`eslint.config.mjs` are gone; the new `tsconfig.json` is a Next.js config.
  </acceptance_criteria>
  <done>Next 16 + Tailwind 4 + shadcn scaffold is in place at repo root with next-intl plugin wired, stripe/resend installed, and stray configs removed.</done>
</task>

<task type="auto">
  <name>Task 2: next-intl v4 routing + proxy.ts middleware + message catalogs</name>
  <files>src/proxy.ts, src/i18n/routing.ts, src/i18n/request.ts, messages/de/common.json, messages/en/common.json</files>
  <read_first>
    @.planning/phases/00-spine-interface-freeze/00-RESEARCH.md (next-intl v4 CITED pattern; Pitfall 6)
    @docs/adr/0012-i18n-next-intl-de-authoritative.md
  </read_first>
  <action>
    Create `src/i18n/routing.ts`, `src/proxy.ts`, and `src/i18n/request.ts` exactly per the `<interfaces>` block (next-intl v4, Next 16 `proxy.ts` filename, locales de/en, defaultLocale de, localePrefix always so bare `/` → `/de`).

    Create `messages/de/common.json` (AUTHORITATIVE) and `messages/en/common.json` with identical key sets — seed with shell keys only (e.g. `appShell.tagline`, `appShell.localeSwitch.de`, `appShell.localeSwitch.en`). The brand wordmark string `anchor` MUST NOT appear in any locale file (DEC-0012 / FR-080) — it is rendered by the Wordmark component. ICU syntax only.

    Do NOT create `src/middleware.ts` — on Next 16 it is ignored; the file is `proxy.ts`.
  </action>
  <verify>
    <automated>grep -q 'createMiddleware(routing)' src/proxy.ts && grep -q 'defineRouting' src/i18n/routing.ts && ! test -f src/middleware.ts && node -e "const de=require('./messages/de/common.json'),en=require('./messages/en/common.json');const k=o=>Object.keys(JSON.parse(JSON.stringify(o)));const flat=(o,p='')=>Object.entries(o).flatMap(([key,v])=>typeof v==='object'?flat(v,p+key+'.'):[p+key]);const a=flat(de).sort(),b=flat(en).sort();process.exit(JSON.stringify(a)===JSON.stringify(b)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/proxy.ts` exports `createMiddleware(routing)` with the matcher; NO `src/middleware.ts` exists.
    - `src/i18n/routing.ts` uses `defineRouting` with locales `['de','en']`, defaultLocale `de`, localePrefix `always`.
    - `src/i18n/request.ts` uses `getRequestConfig` with `requestLocale` + `hasLocale`.
    - `messages/de/common.json` and `messages/en/common.json` have IDENTICAL flattened key sets (the inline node check passes) and neither contains the literal wordmark `anchor`.
  </acceptance_criteria>
  <done>next-intl v4 routing is wired via proxy.ts; DE/EN catalogs have key parity; no stray middleware.ts.</done>
</task>

<task type="auto">
  <name>Task 3: App shell layout + brand components (Flower-of-Life + Unbounded wordmark)</name>
  <files>src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx, src/components/brand/FlowerIcon.tsx, src/components/brand/Wordmark.tsx</files>
  <read_first>
    DESIGN.md brand system (FlowerIcon SVG, .spin-slow, Unbounded weight 900, layout gap-2.5, size-7, text-primary-500)
    @.planning/phases/00-spine-interface-freeze/00-RESEARCH.md (Recommended Project Structure; Pitfall 7 bundle budget)
  </read_first>
  <action>
    Create `src/components/brand/FlowerIcon.tsx` (verbatim from `<interfaces>`, exported component, stroke-only, `currentColor`). Create `src/components/brand/Wordmark.tsx`: renders the `.spin-slow` FlowerIcon in `text-primary-500 size-7` + the Unbounded wordmark `anchor` (literal in the component, NOT a locale string) in `font-display text-xl font-black tracking-tight lowercase`, with `gap-2.5` per DESIGN.md. Accept a `wordmark` prop defaulting to `anchor` for reuse.

    Create `src/app/[locale]/layout.tsx`: load Unbounded (weight 900) via `next/font/google` with `display:swap`+`preload`, set it as the `font-display` CSS var, render `<html lang={locale}>`, wrap children in `NextIntlClientProvider` (await `params.locale`, validate with `hasLocale` → `notFound()` on invalid), and render the `Wordmark` in a minimal shell header. Keep it lean for the Lighthouse first-load-JS budget (<90KB landing) — only Unbounded weight 900, no heavy magic-ui yet.

    Create `src/app/[locale]/page.tsx`: a minimal RSC home/onboarding entry that reads `useTranslations('appShell')` server-side and shows the tagline — no feature logic. This is the Phase-0 landing the Lighthouse budget runs against.
  </action>
  <verify>
    <automated>grep -q 'NextIntlClientProvider' "src/app/[locale]/layout.tsx" && grep -q 'Unbounded' "src/app/[locale]/layout.tsx" && grep -q 'currentColor' src/components/brand/FlowerIcon.tsx && grep -q 'spin-slow' src/components/brand/Wordmark.tsx && grep -rq "anchor" src/components/brand/Wordmark.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `FlowerIcon.tsx` is the 7-circle SVG using `currentColor`, stroke-only.
    - `Wordmark.tsx` renders the spin-slow FlowerIcon (`text-primary-500 size-7`) + Unbounded `font-black tracking-tight lowercase` wordmark with the literal `anchor` string (NOT from a locale file).
    - `layout.tsx` loads Unbounded weight 900 via `next/font/google`, validates locale via `hasLocale`, wraps children in `NextIntlClientProvider`, and renders the Wordmark.
    - `page.tsx` renders a translated tagline with no feature logic.
  </acceptance_criteria>
  <done>The /de and /en app shell renders with Rudolpho-AI branding; the wordmark is component-rendered, locale strings are catalog-driven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js RSC | Untrusted locale path segment crosses into routing/RSC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-04 | Tampering | Arbitrary `[locale]` path segment | mitigate | `layout.tsx` validates the segment with `hasLocale(routing.locales, locale)` → `notFound()`; `request.ts` falls back to `defaultLocale` for unknown locales — no unvalidated locale reaches a dynamic message import. |
| T-00-05 | Information Disclosure | Service-role / secrets in webapp bundle | mitigate | This plan introduces NO secrets; only the anon-key-safe surface is scaffolded. The service-role key + x-hermes-secret belong to Hermes only (enforced in 00-05/00-06), never webapp env. |
</threat_model>

<verification>
- `pnpm build` (or `pnpm next build`) compiles the scaffold.
- `src/proxy.ts` active (not middleware.ts); DE/EN key parity passes the inline node check.
- Brand components render the Flower-of-Life + Unbounded wordmark; wordmark literal is in the component, not a locale file.
- `package.json` lists `stripe` + `resend` (downstream 00-05 deps installed at the scaffold layer).
</verification>

<success_criteria>
A logged-out visitor hitting `/` is redirected to `/de` and sees the Anchor app shell with Rudolpho-AI branding (spinning Flower-of-Life + Unbounded `anchor` wordmark); `/en` renders the English shell; locale strings come from key-parity'd catalogs; the scaffold is lean enough to stay under the Lighthouse landing budget; the runtime deps the phase needs (incl. stripe/resend) are installed.
</success_criteria>

<output>
After completion, create `.planning/phases/00-spine-interface-freeze/00-02-SUMMARY.md` recording the scaffold structure, pinned versions actually installed (incl. React peer + stripe/resend), the next-intl v4 wiring, and the brand component locations.
</output>
</output>
